# Port targets — `tokio-util-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tokio-util` that codex-rs uses, and therefore the minimum subset that `tokio-util-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-160] (rank `19`, tier `direct`, status `To Do`)

**Cargo package name**: `tokio-util`
**Rust module name (in `use` statements)**: `tokio_util`

## Summary

- **8** distinct symbols imported by codex-rs
- **68** total `use` statements
- **60** codex source files affected
- **11** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tokio_util::sync::CancellationToken`

- **56 `use` site(s)** across **8 codex member crate(s)**: `codex-app-server`, `codex-app-server-transport`, `codex-async-utils`, `codex-code-mode`, `codex-core`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/mod.rs:17` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/remote_control/client_tracker.rs:21` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/remote_control/mod.rs:28` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/remote_control/tests.rs:57` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/remote_control/websocket.rs:51` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/unix_socket.rs:15` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/unix_socket_tests.rs:22` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server-transport/src/transport/websocket.rs:41` (`codex-app-server-transport`) — `use tokio_util::sync::CancellationToken`
- `app-server/src/command_exec.rs:688` (`codex-app-server`) — `use tokio_util::sync::CancellationToken`
- `app-server/src/lib.rs:62` (`codex-app-server`) — `use tokio_util::sync::CancellationToken`
- `app-server/src/request_processors.rs:406` (`codex-app-server`) — `use tokio_util::sync::CancellationToken`
- `app-server/src/transport.rs:12` (`codex-app-server`) — `use tokio_util::sync::CancellationToken`
- `async-utils/src/lib.rs:3` (`codex-async-utils`) — `use tokio_util::sync::CancellationToken`
- `code-mode/src/service.rs:12` (`codex-code-mode`) — `use tokio_util::sync::CancellationToken`
- `codex-mcp/src/connection_manager.rs:64` (`codex-mcp`) — `use tokio_util::sync::CancellationToken`
- `codex-mcp/src/rmcp_client.rs:62` (`codex-mcp`) — `use tokio_util::sync::CancellationToken`
- `core/src/client.rs:102` (`codex-core`) — `use tokio_util::sync::CancellationToken`
- `core/src/client_common.rs:16` (`codex-core`) — `use tokio_util::sync::CancellationToken`
- `core/src/codex_delegate.rs:30` (`codex-core`) — `use tokio_util::sync::CancellationToken`
- `core/src/compact_remote.rs:31` (`codex-core`) — `use tokio_util::sync::CancellationToken`
- _… and 36 more (see `codex_external_usage.json` for the full list)._

### `tokio_util::task::AbortOnDropHandle`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/state/turn.rs:10` (`codex-core`) — `use tokio_util::task::AbortOnDropHandle`
- `core/src/tasks/mod.rs:14` (`codex-core`) — `use tokio_util::task::AbortOnDropHandle`
- `core/src/tools/parallel.rs:7` (`codex-core`) — `use tokio_util::task::AbortOnDropHandle`

### `tokio_util::task::TaskTracker`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-app-server`, `codex-exec-server`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server/src/connection_rpc_gate.rs:4` (`codex-app-server`) — `use tokio_util::task::TaskTracker`
- `app-server/src/request_processors.rs:407` (`codex-app-server`) — `use tokio_util::task::TaskTracker`
- `exec-server/src/server/handler.rs:12` (`codex-exec-server`) — `use tokio_util::task::TaskTracker`

### `tokio_util::io::ReaderStream`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-api`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-api/src/files.rs:12` (`codex-api`) — `use tokio_util::io::ReaderStream`
- `codex-api/src/sse/responses.rs:25` (`codex-api`) — `use tokio_util::io::ReaderStream`

### `tokio_util::either::Either`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/tools/parallel.rs:5` (`codex-core`) — `use tokio_util::either::Either`

### `tokio_util::time::FutureExt`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/tui/frame_requester.rs:134` (`codex-tui`) — `use tokio_util::time::FutureExt`

### `tokio_util::compat::Compat`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-uds`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `uds/src/lib.rs:182` (`codex-uds`) — `use tokio_util::compat::Compat`

### `tokio_util::compat::FuturesAsyncReadCompatExt`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-uds`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `uds/src/lib.rs:183` (`codex-uds`) — `use tokio_util::compat::FuturesAsyncReadCompatExt`
